# =============================================================================
# Doc Parser API — Variáveis de Ambiente
#
# Copie para .env e ajuste conforme necessário:
#   cp .env.example .env
#
# Todas as variáveis são opcionais. Valores abaixo são os defaults.
# =============================================================================

# --- GPU / Device ---
# Auto-detecta se omitido. Defina "false" para forçar CPU.
# DOC_PARSER_USE_GPU=true

# --- OCR Engine ---
# "doctr" (deep learning, requer GPU) ou "tesseract" (LSTM, CPU)
# DOC_PARSER_OCR_ENGINE=doctr

# --- OCR Configurações ---
# DPI para conversão de PDF → imagem (maior = melhor qualidade, mais lento)
# DOC_PARSER_OCR_DPI=350

# Tamanho do batch para docTR (ajustar conforme VRAM disponível)
# 20 = bom para 20GB VRAM, reduzir se OOM
# DOC_PARSER_OCR_BATCH_SIZE=20

# Confiança mínima para aceitar resultado OCR (0.0 a 1.0)
# Pode ser sobrescrito por request via ?min_confidence=
# DOC_PARSER_MIN_CONFIDENCE=0.3

# --- OCR Orientação (docTR) ---
# DOC_PARSER_ASSUME_STRAIGHT_PAGES=true
# DOC_PARSER_DETECT_ORIENTATION=false
# DOC_PARSER_STRAIGHTEN_PAGES=false

# --- OCR Tesseract ---
# DOC_PARSER_OCR_LANG=por
# DOC_PARSER_TESSERACT_CONFIG=--oem 1 --psm 3

# --- Pós-processamento ---
# Podem ser sobrescritos por request via query params
# DOC_PARSER_OCR_POSTPROCESS=true
# DOC_PARSER_OCR_FIX_ERRORS=true

# --- Paralelização ---
# DOC_PARSER_PARALLEL_ENABLED=true
# DOC_PARSER_PARALLEL_WORKERS=     # vazio = auto (cpu_count)

# --- PDF pesquisável ---
# DOC_PARSER_SEARCHABLE_PDF=true

# --- Debug ---
# DOC_PARSER_VERBOSE=true

# --- Diretórios ---
# DOC_PARSER_OUTPUT_DIR=/app/output
